<div class='container' style="padding-top: 12px; background: white; height: 100vh; width: 100vw; max-width: 500px; padding-bottom: 100px;">
  <% @split.bills.each do |bill| %>
    <%= link_to bill.merchant, split_bill_path(@split, bill) %>
  <% end %>
</div>
