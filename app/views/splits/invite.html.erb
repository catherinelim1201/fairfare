<div class="container" style="margin-top: 20vh;">
  <% if current_user %>
    Who are you?
  <% else %>
    You have been invited to collaborate on a split by <%= @split.user.name %>. <br>
    Please sign up or sign in to accept the invitation. <br> <br>

    <%= link_to 'Sign In', new_user_session_path(code: params[:code]), class: "btn btn-info" %>
    <%= link_to 'Sign Up', new_user_registration_path(code: params[:code]), class: "btn btn-info" %>
  <% end %>
</div>
